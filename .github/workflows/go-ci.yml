name: Go CI

on:
  pull_request:
    branches: ["**"]
  push:
    branches: ["**"]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-build:
    runs-on: ubuntu-latest
    env:
      GOFLAGS: -buildvcs=false
      BUILD_TARGET: ./cmd/nano-banana
      BDD_TEST_CMD: go test -tags=bdd ./tests/bdd/... -count=1
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Download deps
        run: go mod tidy

      - name: Verify go.mod/go.sum unchanged
        run: git diff --exit-code go.mod go.sum

      - name: Check formatting
        run: |
          dirs=(./cmd)
          [ -d ./internal ] && dirs+=(./internal)
          [ -d ./tests ] && dirs+=(./tests)
          unformatted=$(gofmt -l "${dirs[@]}")
          if [ -n "$unformatted" ]; then
            echo "Unformatted files:"
            echo "$unformatted"
            exit 1
          fi

      - name: Vet
        run: go vet ./...

      - name: Setup Python (for prek)
        if: ${{ hashFiles('prek.toml') != '' || hashFiles('.pre-commit-config.yaml') != '' || hashFiles('.pre-commit-config.yml') != '' }}
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install prek
        if: ${{ hashFiles('prek.toml') != '' || hashFiles('.pre-commit-config.yaml') != '' || hashFiles('.pre-commit-config.yml') != '' }}
        run: |
          python -m pip install --upgrade pip
          python -m pip install prek
          prek --version

      - name: Pre-commit checks (prek)
        if: ${{ hashFiles('prek.toml') != '' || hashFiles('.pre-commit-config.yaml') != '' || hashFiles('.pre-commit-config.yml') != '' }}
        run: |
          prek validate-config
          prek run --all-files

      - name: Unit tests
        run: go test ./... -count=1

      - name: BDD tests
        run: |
          if [ -d ./tests/bdd ]; then
            bash -lc "$BDD_TEST_CMD"
          else
            echo "No BDD tests found at ./tests/bdd; skipping"
          fi

      - name: Build
        run: go build -v "$BUILD_TARGET"
